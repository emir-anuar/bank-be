pipeline {

    agent any

    stages{
        stage('Build')  {
            steps {
                sh 'echo "build application"'
                sh 'echo "done building"'
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo "deploying ur shit"'
            }
        }
    }
}
